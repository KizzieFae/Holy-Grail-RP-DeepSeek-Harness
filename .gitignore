# Repo root — Holy Grail product and local-data ignore rules.

# M13.7 — local legacy AutoGen-era tree (migration source only; not tracked architecture)
autogen_rp/

.env
**/.streamlit/secrets.toml

# OS / editor noise (keep repo root clean)
.DS_Store
Thumbs.db
*.swp
*.swo
.vs/

# Local issue drafting / one-off GitHub helpers (not intended as tracked source)
.github/issue_drafts/
.github/_draft_issues_*.md
.github/issue-bodies-workflow-efficiency/

# Python bytecode (root-level scripts / stray caches)
__pycache__/
*.py[cod]

# Canonical Holy Grail Python virtual environment (M13.4)
.venv/

# Root: local headless LLM simulation tee logs (run_scene_simulation_llm.py)
/*_run*_audit.log

# Local IDE / project exports (not canonical repo content)
_proj_items.json

# Maintenance tooling regeneratable JSON (issue86/88 inventory)
tools/maintenance/issue86_*.json
tools/maintenance/issue88_*.json

# Holy Grail product data (M13.1) — user/runtime generated content stays local
data/*
!data/scene_templates/
!data/scene_templates/**
!data/retrieval/
!data/retrieval/**
!data/fixtures/
!data/fixtures/**
data/fixtures/progression_simulation_scenarios/

# Local investigation scratch (headless harness / overlay experiments)
*_val*.txt

# One-off validation/investigation harness scripts (e.g. AppTest drives); not production source
governance/archive/validation-runs/**/_run_*.py

# Temporary research / local notes
memplace.txt

# Round A / agent dry-run local outputs (archives & synthetic fixtures; not canonical)
_dry_run_cohort_archive_source/
_dry_run_cohort_artifacts/

# Cursor / CLI scratch exports (gh issue JSON staging; not canonical repo content)
.cursor/*-pre.json
.cursor/*-clean.json

# Scratch issue bodies / one-off tooling (local only)
_tmp_hier_edit/
_tmp_prog_issues/
.tmp_issues/
.tmp_*
governance/rp-app/_issue_bodies/

# Local vendor / IDE export (not canonical repo content)
deepseek_v4_overview.rtf

# Issue #215 programme scratch at repo root (canonical thread + `governance/archives/issue-215/`)
/_215_body_*.txt
/_215_body_phase*.md
/_215_body_merged.md
/_215_body_curr.txt
/_merge_215*.py
/_assemble*.py
/_build_*.py
/_p*_215_*.md
/_p*_215_*.txt
/_p[789]_*.py
/_p10_*.py
/_peek215.txt
/_fields.json
/_proj_fields.json
/_scan_*.py
/_191_checkpoint.md
